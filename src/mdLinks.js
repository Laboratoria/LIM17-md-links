const {  
  existsRoute,
  absolutePath,
  getFileFolder,
  getLinks,
  linksStatus 
}= require('./index.js')

//Función Principal

const mdLinks = (route, options) =>{
}

  module.exports = {mdLinks};
