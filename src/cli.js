#!/usr/bin/env node
const {mdLinks} = require('./mdLinks.js');

// const [a, b, ...args]= process.argv;
// console.log(args, args[0], args.includes('--validate'))

// mdLinks(args[0], { validate: args.includes('--validate')})

console.log(process.argv)